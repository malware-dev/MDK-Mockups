<Window x:Class="MDK_UI.ImportBlueprintDialogBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MDK_UI"
        mc:Ignorable="d"
        Title="Importing Blueprint" Width="300" WindowStyle="ToolWindow"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        SizeToContent="Height"
        DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}"
        FocusManager.FocusedElement="{Binding ElementName=BlockName}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ProgressBar x:Name="Progress" Grid.Row="0" Grid.ColumnSpan="3" Margin="2" Maximum="100" Height="15" />

        <Button x:Name="btCancel" Grid.Row="1" Grid.Column="2" Margin="2" Click="BtCancel_Click" IsCancel="True">Cancel</Button>
    </Grid>
</Window>
